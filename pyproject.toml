[project]
name = "welcome-back-to-python"
version = "1.0.0"
description = "Comprehensive Python skills refresher with interview preparation focus"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name = "Learning Project"}]
license = {text = "MIT"}

[project.scripts]
skill-check-agent = "skill_check_agent.cli:main"

[tool.uv]
managed = true

[dependency-groups]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "click>=8.0.0",
    "aiohttp>=3.9.0",
]
